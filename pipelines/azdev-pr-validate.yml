pr:
  paths:
    exclude:
    - README.md

variables:
- group: CI-Tokens

steps:
- pwsh: |
    Install-Module -Name Pester -Force -SkipPublisherCheck -Scope CurrentUser -RequiredVersion "5.3.0"
    ./_TestAll.ps1 -TestToken "$(CI-PAT)"
  displayName: Run Pester
  workingDirectory: '$(Build.SourcesDirectory)'
